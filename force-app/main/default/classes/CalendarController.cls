public class CalendarController {
  /*
    @AuraEnabled
    public static List<Program__c> getPublicEvents() {
        // Query events that are marked as public
      //  return [SELECT Id, Subject, StartDateTime, EndDateTime FROM Event WHERE IsPrivate = false];
        return [SELECT Id, Program_Date__c,Faculty_Name__c FRom Program__c ];
    }
    */
}